package com.example.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = JobTestApplication.class)
public class JobTestApplicationTest {

    @Test
    public void contextLoads() {
        // This test method is empty as its purpose is to verify that the application context loads without errors.
    }
}
